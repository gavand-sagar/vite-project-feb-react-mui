import React from 'react'

type Props = {}

export default function LoginForm({ }: Props) {
    let items = ["apple", "mango", "banana"]
    return (
        <div>
            {items}
        </div>
    )
}